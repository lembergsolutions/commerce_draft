# -*- mode: ruby -*-
# vi: set ft=ruby :

# This is proxy Vagrantfile.
#
# 1. Copy this file to the root folder of your project and rename to Vagrantfile
# 2. Set correct path to the main Vagrantfile

# Define project base path.
PROJECT_BASE_PATH = File.dirname(File.expand_path(__FILE__))

# Load Draft Environment main Vagrantfile. By default, assume that vendor dir is
# "vendor".
load File.dirname(__FILE__) + "/vendor/lemberg/draft-environment/Vagrantfile"
